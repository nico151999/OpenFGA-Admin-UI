apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfga-admin-ui.fullname" . }}
  labels:
    {{- include "openfga-admin-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openfga-admin-ui.selectorLabels" . | nindent 4 }}
